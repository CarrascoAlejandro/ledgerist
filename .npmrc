# Actually enforce the engines field rather than merely warning about it.
# @capacitor/cli requires Node >=22; without this, an install on Node 20
# succeeds with a warning and then fails later inside `cap`.
engine-strict=true
