{
  "name": "@ledger/mobile",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "add:android": "cap add android",
    "sync": "npm run build -w @ledger/web && cap sync android",
    "android": "npm run build -w @ledger/web && cap sync android && cap run android",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor-community/sqlite": "^8.1.0",
    "@capacitor/android": "^8.1.0",
    "@capacitor/barcode-scanner": "^3.0.2",
    "@capacitor/core": "^8.1.0",
    "@capacitor/ios": "^8.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0"
  }
}
