{
  "name": "@ledger/mobile",
  "version": "0.1.2",
  "private": true,
  "scripts": {
    "add:android": "cap add android && node prepare-android.mjs",
    "sync": "npm run build -w @ledger/web && cap sync android",
    "android": "npm run build -w @ledger/web && cap sync android && cap run android",
    "open:android": "cap open android",
    "prepare:android": "node prepare-android.mjs"
  },
  "dependencies": {
    "@capacitor-community/sqlite": "^8.1.1",
    "@capacitor/android": "^8.5.2",
    "@capacitor/barcode-scanner": "^3.1.2",
    "@capacitor/core": "^8.1.0",
    "@capacitor/ios": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.1.0"
  },
  "license": "GPL-3.0-or-later"
}
